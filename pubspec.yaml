name: notova
description: "Tu app de productividad gamificada"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cloud_firestore: ^6.1.3
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  google_sign_in: ^7.1.0
  image_picker: ^1.1.2
  googleapis: ^13.2.0
  http: ^1.2.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

assets:
  - assets/images/
