name: notova
description: "Tu app de productividad gamificada"
publish_to: "none"
version: 0.2.0+2

environment:
  sdk: ^3.11.1

dependencies:
  cloud_firestore: ^6.1.3
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0
  firebase_storage: ^13.1.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  google_sign_in: ^7.1.0
  image_picker: ^1.1.2
  image_cropper: ^9.1.0
  googleapis: ^13.2.0
  http: ^1.2.2
  provider: ^6.1.5+1
  audioplayers: ^6.0.0
  path_provider: ^2.1.0
  csv: ^6.0.0
  share_plus: ^13.0.0
  shared_preferences: ^2.3.0
  drift: ^2.27.0
  sqlite3_flutter_libs: ^0.5.32
  path: ^1.9.1
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.0
  drift_dev: ^2.27.0
  build_runner: ^2.4.15
  mocktail: ^1.0.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logoapp.png"
  adaptive_icon_background: "#120E1A"
  adaptive_icon_foreground: "assets/images/logo_sinfondo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audio/
